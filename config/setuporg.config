MGMT_SERVER_1_DC1=10.65.188.21
MGMT_SERVER_2_DC1=10.65.188.34
MGMT_SERVER_1_DC2=10.71.178.21
MGMT_SERVER_2_DC2=10.71.178.34

REGION_DC1="dc-1"
REGION_DC2="dc-2"

MP_POD_DC1="gateway-1"
MP_POD_DC2="gateway-2"

# From our definition pool, get this definition needed.
# 'dc' is injected by ansible to provide context to this file.
MGMT_SERVER=$MGMT_SERVER_1_DC1

# The Message Processor and Router pod.
MP_POD=$MP_POD_DC1

# The name of the region, corresponding to the data center name.
REGION=$REGION_DC1

# Set Edge sys admin credentials.
ADMIN_EMAIL=ApigeeAdmin@west.com
APIGEE_ADMINPW=Westpl@teng2306

# Management Server information.
MSIP=$MGMT_SERVER

# Specify organization name.
ORG_NAME=west  # lowercase only, no spaces, underscores, or periods.

# Specify the organization administrator user.
# Either specify an existing user, or specify the information
# necessary to create a new user.
# Do not use the sys admin as the organization administrator.
#
# Create a new user for the organization administrator.
NEW_USER="y"
# New user information if NEW_USER="y".
USER_NAME=bxghosh@west.com
FIRST_NAME=
LAST_NAME=
USER_PWD=""
ORG_ADMIN=bxghosh@west.com
#
# Or, specify an existing user as the organization admin,
# omit USER_NAME, FIRST_NAME, LAST_NAME, USER_PWD.
# NEW_USER="n"
# ORG_ADMIN=existing@user.com

# Specify environment name.
ENV_NAME=test

# Specify virtual host information.
VHOST_PORT=9001
VHOST_NAME=default

# If you have a DNS entry for the virtual host.
VHOST_ALIAS=test-apimarketplace.west.com

# If you do not have a DNS entry for the virtual host,
# specify the IP and port of each router as a space-separated list:
# VHOST_ALIAS="firstRouterIP:9001 secondRouterIP:9001"

# Optionally configure TLS/SSL for virtual host.
# VHOST_SSL=y     # Set to "y" to enable TLS/SSL on the virtual host.
# KEYSTORE_JAR=   # JAR file containing the cert and private key.
# KEYSTORE_NAME=  # Name of the keystore.
# KEYSTORE_ALIAS= # The key alias.
# KEY_PASSWORD=   # The key password, if it has one.

# Specify the analytics group.
# AXGROUP=axgroup-001 # Default name is axgroup-001.
